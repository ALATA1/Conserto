pipeline {
    agent any

    environment {
        // Facultatif : chemin vers Python et Scripts si non global
        // PATH = "C:\\Python311\\;C:\\Python311\\Scripts\\;%PATH%"
        EMAIL_TO = 'ibrahima.alata@conserto.pro'
    }

    stages {
        stage('Préparation') {
            steps {
                echo 'Installation des dépendances Python'
                // bat 'pip install -r requirements.txt'
            }
        }

        stage('Exécution des tests Robot Framework') {
            steps {
                echo 'Lancement des tests Robot'
                // bat 'robot --outputdir results tests'
            }
        }

        stage('Archivage des résultats') {
            steps {
                echo 'Archivage des résultats Robot Framework'
                // archiveArtifacts artifacts: 'results/**/*.*', allowEmptyArchive: true
            }
        }

        stage('Rapport Robot (si plugin installé)') {
            steps {
                // robot outputPath: 'results'
                echo 'Archivage des résultats Robot Framework'
            }
        }
    }

    post {
        always {
            echo 'Pipeline terminé'
        }
    }
}
